import GlobalStyle from './Global';
import styled, {
  createGlobalStyle,
  css,
  keyframes,
  ThemeProvider
} from './config';
import Theme from './Theme';
export { Primary } from './variants/Primary';
export { Secondary } from './variants/Secondary';
export { Theme, css, createGlobalStyle, keyframes, ThemeProvider, GlobalStyle };

export default styled;
