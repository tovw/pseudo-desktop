import React, { FC } from 'react';
import styled from '../theme';

const StyledTaskbarIcon = styled('div')``;

export const TaskbarIcon: FC = () => {
  return <StyledTaskbarIcon></StyledTaskbarIcon>;
};
